/** @type {import("next").NextConfig} */
const nextConfig = {
  // Nếu trước đó bạn có cấu hình ESLint bỏ qua khi build, để lại dòng dưới:
  // eslint: { ignoreDuringBuilds: true },
};

export default nextConfig;
